# Optional: set in Vercel → Project → Environment Variables (Production) when you add a custom domain.
# Use the exact public URL with https, no trailing slash, e.g. https://www.yourdomain.com
# NEXT_PUBLIC_SITE_URL=https://www.example.com
